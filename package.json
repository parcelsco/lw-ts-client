{
  "name": "lw-ts-client",
  "packageManager": "yarn@3.2.3",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && swc src --out-dir dist && tsc --pretty --skipLibCheck --declaration --emitDeclarationOnly",
    "release": "standard-version",
    "lint": "eslint --ext .js,.ts src"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.5.1",
    "@swc/cli": "^0.1.57",
    "@swc/core": "^1.2.242",
    "@types/node": "^18.7.13",
    "@types/prettier": "^2",
    "openapi-typescript-codegen": "^0.23.0",
    "prettier": "^2.7.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  }
}
